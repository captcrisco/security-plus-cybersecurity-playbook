what is a url....
https:// is protocal
www. is subdomain
google.com  domain name
/docs is the path or page
top level domain .com .edu. mil

DNS translates IP addresses to domain names. 
  Records A: 18.93.146.51 to wikipedia.org

Port 53 is utilized by UDP

seven steps 1, Query (request) 2, Recursive 3, Root 4, TLD

Authoritative server host records. file keeping also keeps IP's 

DNS record typese A: map for v4 AAAA: mpa for v6. MX:used for emial. NS: specifies a given authoritative server to refer to

Http is a stateless protocal
Http Https Port 80/443

  
  
